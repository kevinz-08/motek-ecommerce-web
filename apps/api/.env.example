# ── Base de datos ─────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# Máximo de conexiones del pool pg.Pool por proceso (default 5).
# En Railway con un solo proceso se puede subir a 10.
# Si usas el pooler nativo de Neon, añade ?pgbouncer=true a DATABASE_URL y baja esto a 1.
DATABASE_POOL_MAX=5

# ── JWT ───────────────────────────────────────────────────────────────────────
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ── Comunicación interna Next.js ↔ NestJS ─────────────────────────────────────
# Secret compartido entre apps/web y apps/api para el endpoint POST /auth/session-token.
# Debe ser idéntico en AMBAS apps. Mínimo 32 caracteres aleatorios.
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_SECRET=

# ── Google OAuth ──────────────────────────────────────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── Wompi ─────────────────────────────────────────────────────────────────────
WOMPI_PUBLIC_KEY=
WOMPI_PRIVATE_KEY=
WOMPI_INTEGRITY_SECRET=
WOMPI_EVENTS_SECRET=
WOMPI_ENV=sandbox

# ── Email (Resend) ────────────────────────────────────────────────────────────
# IMPORTANTE: el dominio del FROM debe estar verificado en https://resend.com/domains
# o Resend rechaza el envío con HTTP 403 "domain not verified". El default es el
# dominio del comercio en producción.
RESEND_API_KEY=
RESEND_FROM_EMAIL=no-reply@motekstore.com

# ── Imágenes (Cloudinary) ─────────────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ── App ───────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:3000

# ── Vendelo (logística / envíos) ─────────────────────────────────────────────
# API Key del panel de Vendelo. Requerida al arrancar el servidor.
VENDELO_API_KEY=
# URL base de la API (sin slash final). Verificar URL exacta en el panel de Vendelo.
VENDELO_API_URL=https://api.vendelo.co
# Secret compartido para verificar webhooks entrantes del Chatbot Connection (Fase 3).
VENDELO_WEBHOOK_SECRET=
# Saldo mínimo de wallet (en centavos COP) antes de disparar alerta en logs.
VENDELO_WALLET_ALERT_THRESHOLD=50000

# Información de la tienda para el pickup_info de cada orden Vendelo.
# VENDELO_STORE_CITY_CODE = código DIVIPOLA de 8 dígitos de la ciudad de la tienda.
# Ej: 05001000 = Medellín | 11001000 = Bogotá | 76001000 = Cali
VENDELO_STORE_NAME=Motek Store
VENDELO_STORE_PHONE=
VENDELO_STORE_ADDRESS=
VENDELO_STORE_CITY_CODE=
VENDELO_STORE_SUBDIVISION_CODE=

# Dimensiones y peso por defecto para todos los productos en line_items.
# Ajustar según el empaque promedio de los repuestos que se despachan.
VENDELO_DEFAULT_WEIGHT_KG=1
VENDELO_DEFAULT_HEIGHT_CM=25
VENDELO_DEFAULT_WIDTH_CM=25
VENDELO_DEFAULT_LENGTH_CM=10

# Polling de estados de envío contra Vendelo (workaround mientras no abran webhooks).
# Cron consulta GET /v1/admin/orders/{id} para órdenes vivas con vendeloOrderId.
# Defaults sensatos para producción inicial — ajustar si el volumen cambia.
VENDELO_POLL_ENABLED=true
VENDELO_POLL_INTERVAL_MS=300000
VENDELO_POLL_BATCH_SIZE=50
VENDELO_POLL_REQUEST_DELAY_MS=100

# ── Observabilidad (Sentry) ───────────────────────────────────────────────────
# Preparado para cuando se instale @sentry/nestjs.
# Si no está definido, el filtro de errores loguea a Railway con errorId para correlación.
# Obtener en: https://sentry.io → Settings → Projects → <proyecto> → Client Keys
SENTRY_DSN=
