{
  "name": "@motek/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Dominio puro del e-commerce Motek: entidades, interfaces de repositorio, use cases y Result<T,E>. Cero dependencias externas.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"(no lint for @motek/domain yet)\""
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.6",
    "typescript": "^5.9.0",
    "vitest": "^4.1.6"
  }
}
